apiVersion: v1
kind: Service
metadata:
  name: micro1
  namespace: dev
spec:
  type: LoadBalancer 
  selector:
    app: micro1
  ports:
    - name: http
      protocol: TCP
      port: 8888
      targetPort: 8080
